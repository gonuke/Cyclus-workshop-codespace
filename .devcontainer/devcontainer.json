{
    "name": "Cyclus Workshop Test",
    "image" : "mcr.microsoft.com/devcontainers/miniconda:latest",
    "postCreateCommand": "echo 'source /opt/conda/etc/profile.d/conda.sh' >> ~/.bashrc && echo 'conda activate cyclus-workshop' >> ~/.bashrc && conda create -y -n cyclus-workshop python==3.12 && . ~/.bashrc && conda activate cyclus-workshop && conda install -y -c conda-forge cycamore cymetric==1.6.0 icu=75",   
    "customizations": {
        "vscode": {
            "extensions": [
                "redhat.vscode-xml",
                "ms-python.python",
                "qwtel.sqlite-viewer",
                "alexcvzz.vscode-sqlite"
            ]
        }
    }
}